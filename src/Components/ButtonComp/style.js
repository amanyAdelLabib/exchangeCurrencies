import {StyleSheet} from 'react-native';
// import {FONTS, COLORS, hp, wp} from '../../style';

const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
export default styles;
